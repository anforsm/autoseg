{
  evaluation: {
    method: "hagglom",
    results_dir: "results",
    ground_truth_skeletons: "eval/refined_skels.graphml",
    ground_truth_labels: [
      {
        path: "SynapseWeb/kh2015/oblique",
        dataset: "labels/s1"
      }
    ]
  }
}
